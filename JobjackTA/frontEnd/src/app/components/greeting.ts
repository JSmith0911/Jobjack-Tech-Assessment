import { Component } from '@angular/core';
import { componentFactoryName } from '@angular/compiler';

@Component({
    selector: 'app-greeting',
    template: `<h1>Welcome to the directory search!</h1>`
})

export class GreetingComponent {}